# Reconstruction Demo Images

Place a compact multi-camera image subset here before running `quick_start.py`.

Expected layout:

```text
examples/reconstruction_demo/images/
├── cam_left/
│   ├── frame_0001.jpg
│   └── ...
└── cam_right/
    ├── frame_0001.jpg
    └── ...
```

The repository currently keeps only `.gitkeep` placeholders. For the public paper artifact, add a small image subset here or link the dataset in `docs/dataset.md`.
