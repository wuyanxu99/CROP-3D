# Data Directory

This directory is a placeholder for dataset access instructions.

Do not commit raw videos, complete image sequences, COLMAP databases, or full point clouds to Git. Use `docs/dataset.md` for download/request information and keep only compact demo assets under `examples/`.

